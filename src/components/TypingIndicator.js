require("../styles/TypingIndicator.css");

import React from "react";

//Typing Indicator Icon (similar to Apple's)
const TypingIndicator = props => (
  <div className="typing-indicator">
    <span />
    <span />
    <span />
  </div>
);

export default TypingIndicator;
