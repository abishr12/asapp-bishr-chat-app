"use strict";

// Defines where the server is listening.
// Looks for the port where the Bishr Chat API is running (4008)
// Storing the address in the config
// file allows us to easily create different settings for various environments.
export default {
  api: "http://localhost:4008"
};
